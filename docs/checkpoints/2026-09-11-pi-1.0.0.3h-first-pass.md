# DRFXI 1.13 `Update PI 1.0.0.3h` — first bounded pass

Date: 2026-09-11

Scope: only the 1.13 changelog item `Update PI 1.0.0.3h`, using DRFXI 1.12 -> 1.15 because 1.13/1.14 binaries are unavailable. Findings across this interval are not automatically attributed to 1.13.

## Starting constraints

- Do not reopen Intel LAN OPROM, TCC, SMU power-limit internals, S3, DMI, GOP/display, flash EIP/TCG storage.
- Do not reverse-engineer all 181 byte-changed PE modules.
- Normalize PCD-token renumbering, relocations, layout/build noise, and global platform constants before treating a delta as semantic.
- Start from the three named PE modules that are present in 1.15 and absent in the 1.12 named-module set, then follow at most one immediate dependency edge.

## Initial direct binary classification

### `AmdVariableProtection`

- FFS GUID: `0F411BE5-B10C-4DDA-B28C-868FC24789F8`
- FFS type: DXE driver (`0x07`)
- PE subsystem: EFI boot-service driver (`0x0C`)
- image size: 10,752 bytes
- entry point RVA: `0x10C4`
- version section: `1.0`
- DEPEX:
  - `6441F818-6362-4E44-B570-7DBA31DD2453`
  - `13A3F0F6-264A-3EF0-F2E0-DEC512342F34`
  - `6441F818-6362-4E44-B570-7DBA31DD2453`
  - (`81D1675C-86F6-48DF-BD95-9A6E4F0925C3` OR `AF23B340-97B4-4685-8D4F-A3F28169B21D`)

The module is paired in 1.15 with the newly exposed/default-enabled `AMD Variable Protection` setup item, but exact protocol/variable semantics are still being classified in this pass.

### `GenerateTimeBaseVariable`

- FFS GUID: `EDAF7B16-E15D-4A64-9398-E6CAC9F026AF`
- FFS type: Application (`0x09`)
- PE subsystem: EFI application (`0x0A`)
- image size: 692,320 bytes
- entry point RVA: `0x454`
- version section: `1.0`
- no DXE DEPEX section (application rather than DXE driver)

The unusually large application image requires classification from strings/calls before any PI attribution.

### `HardwareSignatureEntry`

- FFS GUID: `B7EE4835-84CE-4B15-BF52-2D11574CE470`
- FFS type: DXE driver (`0x07`)
- PE subsystem: EFI runtime driver (`0x0B`)
- image size: 6,752 bytes
- entry point RVA: `0x2E0`
- version section: `1.0`
- DEPEX: `13A3F0F6-264A-3EF0-F2E0-DEC512342F34`

## Existing platform delta kept separate for now

A real 1.12 -> 1.15 PCI MMCONFIG/ECAM relocation is already established:

```text
1.12: 0xF0000000
1.15: 0xE0000000
```

It occurs at least in `AmdNbioIOMMUDxe` and `PciRootBridge`. This pass will test whether it shares an immediate dependency/component cluster with the three new modules. It is not being linked merely because the timing overlaps.

## Next actions in this pass

1. Resolve DEPEX GUIDs and direct protocol/variable/event dependencies for the two DXE modules.
2. Classify the large `GenerateTimeBaseVariable` application from its strings and Boot/Runtime-service usage.
3. Search 1.12 for equivalent providers/callbacks/variable producers under other module names/GUIDs.
4. Follow only one immediate dependency edge and compare the corresponding functions/modules 1.12 -> 1.15.
